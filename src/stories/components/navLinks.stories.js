import { NavLinks } from "../../components/navLinks";

export default {
  title: "Components/NavLinks Component",
  components: NavLinks,
};

export const primary = {
  render: () => <NavLinks />,
};
